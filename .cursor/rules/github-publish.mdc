---
description: 用户要求将项目发布到 GitHub 时的标准流程（安全、可重复）
alwaysApply: true
---

# 发布到 GitHub

当用户说「上传到 GitHub」「推到 GitHub」「在 GitHub 上开源」等时：

## 安全与凭据

- **不要在对话中索要或让用户粘贴 GitHub 密码**。优先使用本机已登录的凭据。
- 在 Windows 上优先使用 **GitHub CLI**：用户在本机终端执行一次 `gh auth login`（浏览器或 Token），之后 Agent 可用 `gh` 创建仓库并推送。
- 若未安装 `gh`，可提示安装：`winget install GitHub.cli`，或使用 **Git Credential Manager** / **SSH 密钥** 配合 `git push`。
- **不要**在仓库中提交密钥、`.env`、令牌或大型生成的 `dist/` 二进制（本仓库已在 `.gitignore` 中排除常见项）。

## 推荐操作顺序

1. 确认 `.gitignore` 合理；必要时补充 `README.md`、`LICENSE`（开源常用 MIT）。
2. `git init`（若尚无仓库）→ `git add` → `git commit`（仅当用户明确要求提交/推送时再提交）。
3. 检查 `gh auth status`；未登录则指导用户运行 `gh auth login`，不要代填密码。
4. 新建公开仓库并推送（仓库名与用户确认或使用其指定的英文名）：

   ```bash
   gh repo create <repo-name> --public --source=. --remote=origin --push
   ```

   若远程已存在： `git remote add origin ...` 后 `git push -u origin main`（分支名以实际为准）。

5. 将最终仓库 URL 返回给用户。

## 说明

Agent **无法跨会话永久保存**用户的 GitHub 登录态；持久化靠的是用户电脑上的 `gh` 凭据或 SSH agent。用户在新机器上需重新 `gh auth login` 一次。
